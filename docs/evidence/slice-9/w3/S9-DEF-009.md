# S9-DEF-009 — ראיית AuthZ מדויק ו־keyset pagination

תאריך: 27.8.2026

סטטוס: `VERIFIED` על בסיס ה־session הנוכחי בלבד.

סביבה: Supabase מקומי שנבנה מחדש מן ה־migrations ו־Playwright מול Next.js
מקומי. לא נעשתה גישה לפרויקט linked/Hosted, לא הופעל Sports provider חי ולא
נשמרו secrets, cookies, tokens, כתובות Email של fixtures או payloads בראיה.

## החוזה שנבדק

- הרשאת match מדויקת אינה נגזרת עוד מרשימת 200 חברים: הקשר ה־match והליגה
  נבדק עבור המשאב המבוקש, ולכן חבר חוקי נשאר מורשה גם מחוץ לעמוד הראשון;
  משתמש וליגה זרים מקבלים 404 אטום.
- dashboard, בקשות ההצטרפות של המשתמש, תור המנהל, משחקי הליגה, בחירת הליגה
  ל־match, ניחושים שנחשפו ורשימת משחקי מנהל המערכת משתמשים בעמודים bounded עם
  sentinel ו־`hasMore`/cursor במקום להציג hard cap כאילו הוא רשימה מלאה.
- ה־RPCs הציבוריים גוזרים actor מ־`auth.uid()`, משתמשים ב־
  `SECURITY DEFINER` צר עם `search_path = ''`, revoke מ־PUBLIC/anon ו־grant
  רק ל־`authenticated`. תור מנהל דורש הרשאה לליגה המדויקת; בקשות המשתמש אינן
  מקבלות `user_id` מן הלקוח.
- cursors הם keyset יציבים עם tie-breaker UUID. filters של season/status/round
  נשמרים בקישור העמוד הבא; selected league מפורש נבדק בדיוק גם כאשר הוא מחוץ
  לעמוד הבחירה הנוכחי.
- רכיב הדפדוף מציג next/end/empty states, ניתן להפעלה במקלדת, שומר RTL ואינו
  יוצר overflow ב־Desktop Chrome או Pixel 5. יעדי ההפעלה שנבדקו הם לפחות
  44px.
- לא נוסף index ספקולטיבי. בדיקות scale המקומיות מוכיחות 101/201+ reachability
  וחוזי bound; אין במסמך זה claim על Hosted query plan או Advisor.

## regressions חדשים

- `supabase/tests/keyset-pagination.test.sql`: ‏68 assertions ל־ACL, actor
  derivation, exact/cross-league AuthZ, bounds, filters, cursor tie-breakers,
  member/request #101 ומשחקים/חברים מעל hard caps הישנים.
- `supabase/tests/keyset-pagination-concurrency.test.sql`: ‏21 assertions עם
  שלושה חיבורי dblink מחויבים. מנהל דוחה את שורת ה־cursor בין קריאת עמוד ראשון
  לשני; traversal עם timestamp זהה ממשיך בלי gap או duplicate, עם המתנה
  bounded, drain ו־cleanup מחויב.
- `src/lib/keyset-query-contracts.test.ts`: actor-derived RPCs, sentinel bounds,
  filters ו־exact selected league שאינו בעמוד הבחירה.
- `e2e/pagination.spec.ts`: dashboard, own requests, manager queue, admin
  matches, league matches, eligible leagues, revealed predictions, filters,
  empty/end states ו־cross-league opaque denial בשני viewports.

## פקודות ותוצאות שהורצו בפועל

### מסד ממוקד

```powershell
npx.cmd --no-install supabase test db supabase/tests/keyset-pagination.test.sql
```

עבר: `Files=1, Tests=68`, ‏`Result: PASS`.

הרצה ראשונה של קובץ המקביליות נכשלה באמת: PostgreSQL דיווח שאין aggregate
`min(uuid)`. ה־assertion שונה ל־`ORDER BY ... LIMIT 1`, ללא cast שמשנה את
סדר ה־UUID, ואז הורץ:

```powershell
npx.cmd --no-install supabase test db supabase/tests/keyset-pagination-concurrency.test.sql
```

עבר: `Files=1, Tests=21`, ‏`Result: PASS`; assertions בקובץ כוללים cleanup
מחויב וסגירת שלושת החיבורים.

אימות הסגירה המשותף לשני הקבצים:

```powershell
npx.cmd --no-install supabase test db supabase/tests/keyset-pagination.test.sql supabase/tests/keyset-pagination-concurrency.test.sql
```

```text
Files=2, Tests=89
All tests successful.
Result: PASS
```

### דפדפן

```powershell
npm.cmd run test:e2e -- e2e/pagination.spec.ts
```

ה־build וסריקת 51 client artifacts עברו, אך שני הפרויקטים נכשלו לפני הזרימה:
SQL fixture גדול הועבר כ־Windows command argument ועבר את מגבלת האורך. ה־helper
שונה ל־`docker exec --interactive psql` עם SQL ב־stdin. ריצת Desktop ממוקדת
נוספת נכשלה משום שהבדיקה חיפשה hidden `input[name=matchId]`, בעוד הגבול
השרתִי חושף רק `data-manual-match-id`; ה־locator תוקן לחוזה האמיתי.

```powershell
npm.cmd run test:e2e:run -- e2e/pagination.spec.ts --project=desktop-chromium
npm.cmd run test:e2e:run -- e2e/pagination.spec.ts --project=mobile-chromium
```

התוצאות החוזרות: Desktop ‏`1 passed (12.5s)`; Pixel 5 ‏`1 passed (12.4s)`.

פקודת סגירת W3, הכוללת במפורש את `e2e/settings.spec.ts`, הייתה:

```powershell
npm.cmd run test:e2e:run -- e2e/scoring.spec.ts e2e/sync.spec.ts e2e/settings.spec.ts e2e/pagination.spec.ts
```

הריצה הראשונה הסתיימה ב־`7 passed, 1 failed`: settings ב־Desktop השאיר fixture
`completed`, ולכן scoring המאוחר ב־Pixel 5 נחסם וה־cleanup שלו נכשל. לאחר
תיקון cleanup של DEF-007 הורץ:

```powershell
npx.cmd --no-install supabase db reset
npm.cmd run test:e2e:run -- e2e/scoring.spec.ts e2e/sync.spec.ts e2e/settings.spec.ts e2e/pagination.spec.ts
```

האיפוס החיל בהצלחה את כל ה־migrations עד migration ה־pagination; הריצה החוזרת
עברה `8 passed (1.1m)`.

### שערי סגירה מלאים

```powershell
npm.cmd run lint
npm.cmd run typecheck
npm.cmd run test
npm.cmd run test:db
```

```text
lint: PASS, exit 0
typecheck: PASS, exit 0
Vitest: 40 files passed; 563 tests passed
pgTAP: 19 files; 1182 tests; Result: PASS
```

```powershell
npm.cmd run types:check
```

הריצה הראשונה נכשלה עם `Generated database types are stale`. הסיבה הייתה
הסרת שורת הסיום שה־generator עצמו מפיק. בוצע תיקון רק דרך הפקודות הנתמכות:

```powershell
npm.cmd run types:db
npm.cmd run typecheck
npm.cmd run types:check
```

שלושתן עברו; התוצאה האחרונה הייתה
`Generated database types are current`.

## commits שנדחפו

- `9c5e469` — גבול המימוש `fix(W3): S9-DEF-009`.
- `0f8455d` — pgTAP מקבילי, query contracts ו־Playwright pagination.
- `ee5fb79` — generated types מדויקים מול מסד ה־migrations המקומי.

אין blockers מקומיים פתוחים ל־DEF-009. W4 לא הותחל במסגרת session זה.
