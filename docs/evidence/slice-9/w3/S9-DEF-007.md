# S9-DEF-007 — ראיית עריכת הגדרות ליגה

תאריך: 27.8.2026

סביבה: Supabase מקומי חד־פעמי ו־Next.js production build מקומי. בדיקות
Playwright רצו מול השרת המקומי ב־Desktop Chrome וב־Pixel 5, ללא trace,
screenshot או video. לא נעשתה גישה לפרויקט linked/Hosted, לא נכתב ledger,
ולא נשמרו secret, cookie, token או Email בראיה.

## החוזה שמומש

- מנהל הליגה, או מנהל מערכת שקיבל הרשאה מפורשת, קוראים ליגה אחת בלבד דרך
  `public.get_editable_league_settings(uuid)`. ה־RPC אינו מוסיף policy רחבה
  למנהל מערכת, אינו חושף ליגות זרות ב־dashboard, ומחזיר תפקיד עורך, גרסאות,
  ניקוד, פרסי Demo, kickoff ראשון, latch וזמן מסד באותה תמונת statement.
  קישור הזמנה והניווט המלא נשארים למנהל הליגה בלבד.
- `public.update_league_settings(...)` הוא גבול `SECURITY DEFINER` צר עם
  `search_path = ''`, שמאמת payload מלא לפני נעילות, גוזר actor מ־`auth.uid()`,
  ומחזיר תוצאה אטומית עם `settings_version`, `scoring_version` ו־`changed`.
  אין UPDATE ישיר ל־`authenticated`, ואין EXECUTE ל־PUBLIC או `anon`.
- שם, תיאור, סכום והוראות Demo, מועד סגירת הצטרפות ו־late join ניתנים לעריכה
  עד completion/archive. שינוי פרטים בלבד אינו נועל matches. ליגה שהושלמה או
  הועברה לארכיון היא לקריאה בלבד.
- שינוי ניקוד או פרסי Demo נועל תחילה את שורת העונה (`FOR UPDATE`), ולכן FK
  `KEY SHARE` מסדר גם insert או העברת match לעונה; אחר כך matches ננעלים בסדר
  UUID. רק לאחר ההמתנה מחושבים kickoff ראשון ו־latch ונקרא
  `clock_timestamp()`. מעבר ל־active/completed/archived, kickoff שהגיע,
  `predictions_locked_at`, או match live/finished הם latch בלתי הפיך משותף
  לניקוד ולפרסים. match במצב `canceled` נועל רק כאשר כבר נשמר בו
  `predictions_locked_at`; הסטטוס לבדו אינו latch.
- מערכת הגרסאות דוחה stale mutation, אך replay סמנטי זהה הוא no-op זול:
  הגרסה נשארת זהה ולא נכתב audit נוסף. שתי הגשות זהות מקביליות מסתיימות
  במוטציה אחת ו־no-op אחד בלבד.
- `joins_close_at` נאכף כ־UTC סופי גם ב־Zod/RPC וגם ב־CHECK מסדי מאומת;
  שנת `0000` ו־`+/-infinity` נדחות. קלט ה־UTC הוא text מפורש התומך עד שש
  ספרות שבר, משום ש־Chromium מאפס `datetime-local` עם microseconds. שמירה של
  פרט אחר משמרת בדיוק ערך כגון `.123456`.
- הטופס שומר draft מקומי נשלט לכל השדות, checkbox, ניקוד ושורות הפרס. לכן
  reset אוטומטי של React Form Actions אינו מוחק תיקוני משתמש אחרי validation;
  גרסאות הפעולה נשמרות בנפרד בכל שגיאת validation/auth/service, והגרסה שחזרה
  מהמסד משמשת להגשה הבאה.
- `private.touch_updated_at()` משתמש ב־`clock_timestamp()`. עדכון חוקי התחרות,
  גרסאות scoring/settings, החלפת מסמך הפרסים ו־audit מתבצעים באותה transaction.

## כיסוי מסדי

`supabase/tests/league-settings.test.sql` מכסה 56 assertions: מבנה ו־ACL,
היעדר UPDATE ישיר או admin SELECT רחב, manager/admin ומקרי anonymous/member/
foreign/missing/revoked, עדכון אטומי, גרסאות ו־audit, replay/stale, מסמכי פרס
חסרים/עודפים/לא רציפים, סדר וטווח ניקוד, גבולות טקסט וקישור תשלום, גרסאות
`NULL`/לא חוקיות, UTC לא סופי/שנת אפס/microseconds, השפעת מועד ההצטרפות,
latch, status ו־completed/archive.

`supabase/tests/league-settings-concurrency.test.sql` מכסה 75 assertions עם
dblink וחיבורים מחויבים אמיתיים:

- כותבים מתחרים: winner יחיד ו־stale loser;
- שתי הגשות זהות במקביל: mutation אחד, no-op אחד ו־audit אחד;
- payload חסר נדחה לפני שורת league נעולה;
- שינוי פרטים מסתיים בזמן ששורת match נעולה;
- fixture-first ו־settings-first מוכיחים serialization דרך שורת season;
- waiter שמתחיל לפני kickoff ומוחזק מעבר לגבול קורא זמן מסד טרי אחרי השחרור
  ונדחה;
- latch שנוסף במקביל נצפה; וביטול הרשאת מנהל מערכת ממתין ל־`FOR KEY SHARE`
  ואחר כך מתקבלת דחייה אטומה.

## כיסוי TypeScript ודפדפן

- Vitest: 45 בדיקות validator/formatter/lock/error עברו, כולל microseconds,
  שנת אפס, זמן לא סופי, payload וגרסאות לא חוקיות.
- Playwright: התרחיש היחיד רץ בשני הפרויקטים (2/2) ומכסה RTL וללא overflow,
  validation ולאחריו version carry, שמירות לגרסה 2 ו־3, replay זהה שנשאר ב־3,
  reload ששומר `.123456`, outsider עם 404 אטום, admin ללא dashboard pollution
  וללא invite controls, active עם חוקים לקריאה בלבד ופרטים ניתנים לעריכה,
  completed לקריאה בלבד, Tab focus ויעדי מגע בגודל 44px לפחות לכפתורי שמירה,
  הוספה והסרת פרס.

## פקודות ותוצאות סופיות

Node של ה־workspace הופעל ישירות משום ש־`node`/`npm` אינם ב־PATH של shell זה:

```powershell
$runtimeBin = 'C:\Users\Tal\.cache\codex-runtimes\codex-primary-runtime\dependencies\node\bin'
$predictorNode = Join-Path $runtimeBin 'node.exe'
$env:Path = "$runtimeBin;$env:Path"
```

ה־reset המקומי המדויק, ללא `--linked`, היה:

```powershell
& .\node_modules\.bin\supabase.cmd db reset
```

```text
Applying migration 20260826300000_slice9_editable_league_settings.sql...
Finished supabase db reset on branch feature/slice-9-implementation.
```

```powershell
& .\node_modules\.bin\supabase.cmd test db supabase/tests/league-settings.test.sql supabase/tests/league-settings-concurrency.test.sql
```

```text
league-settings.test.sql .............. ok
league-settings-concurrency.test.sql .. ok
All tests successful.
Files=2, Tests=131
Result: PASS
```

```powershell
& $predictorNode node_modules/vitest/vitest.mjs run src/features/leagues/leagues-rules.test.ts
```

```text
Test Files  1 passed (1)
Tests  45 passed (45)
```

```powershell
& $predictorNode node_modules/typescript/bin/tsc --noEmit
& $predictorNode node_modules/eslint/bin/eslint.js `
  src/features/leagues/actions.ts `
  src/features/leagues/components/league-settings-form.tsx `
  src/features/leagues/components/league-tabs.tsx `
  src/features/leagues/errors.ts `
  src/features/leagues/leagues-rules.test.ts `
  src/features/leagues/schemas.ts `
  src/features/leagues/service.ts `
  src/features/leagues/settings-query.ts `
  src/features/leagues/settings-types.ts `
  'src/app/(app)/leagues/[leagueId]/settings/page.tsx' `
  e2e/settings.spec.ts
```

שתי הפקודות הסתיימו ב־exit 0 ללא findings. פקודת ההשוואה המדויקת בין schema
מקומי לקובץ generated, ללא כתיבת קובץ זמני, הייתה:

```powershell
$generated = (& .\node_modules\.bin\supabase.cmd gen types typescript --local | Out-String)
if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
$current = Get-Content -LiteralPath src/types/database.generated.ts -Raw
$normalizedGenerated = $generated.Replace("`r`n", "`n")
$normalizedCurrent = $current.Replace("`r`n", "`n")
if ($normalizedGenerated -ne $normalizedCurrent) {
  Write-Error 'Generated database types are stale.'
  exit 1
}
Write-Output 'Generated database types are current.'
```

```text
Connecting to db 5432
Generated database types are current.
```

```powershell
& $predictorNode --import tsx scripts/validate-env.ts
& $predictorNode node_modules/next/dist/bin/next build
```

```text
Compiled successfully
Finished TypeScript
Generated static pages (5/5)
```

```powershell
$env:PLAYWRIGHT_BASE_URL = 'http://localhost:3000'
& $predictorNode --env-file=.env.local node_modules/@playwright/test/cli.js test e2e/settings.spec.ts --workers=1
```

```text
[desktop-chromium] editable league settings ... passed
[mobile-chromium] editable league settings ... passed
2 passed (18.5s)
```

בדיקת whitespace המדויקת הייתה:

```powershell
git diff --check
```

היא הסתיימה ב־0; הודפסו warnings של LF/CRLF בלבד.

## קבצי DEF-007

- מסד: `supabase/migrations/20260826300000_slice9_editable_league_settings.sql`,
  `supabase/tests/league-settings.test.sql`,
  `supabase/tests/league-settings-concurrency.test.sql`.
- גבול feature: `src/features/leagues/actions.ts`, `errors.ts`, `schemas.ts`,
  `service.ts`, `settings-query.ts`, `settings-types.ts`,
  `components/league-settings-form.tsx`, `components/league-tabs.tsx`,
  `leagues-rules.test.ts`.
- UI/browser: `src/app/(app)/leagues/[leagueId]/settings/page.tsx`,
  `e2e/settings.spec.ts`.
- חוזה ותפעול: `README.md`, `docs/product.md`, `docs/architecture.md`,
  `docs/technical-plan.md`, `docs/security.md`, `docs/testing.md`,
  `docs/scale.md`.
- generated משותף: `src/types/database.generated.ts` משקף את כל migrations
  המקומיות הפעילות, כולל עבודה מקבילה; אין claim שכל diff בו שייך ל־DEF-007.

## גבולות

- אין claim על Hosted/Production. הראיה מקומית בלבד ובמצב Demo.
- קבצי pagination ב־`src/features/leagues/queries.ts` ו־`types.ts` הם עבודה
  מקבילה ואינם חלק ממימוש DEF-007.

## אימות סגירת W3 טרי — 27.8.2026

הסטטוס `VERIFIED` מבוסס רק על ההרצות הבאות מה־session הנוכחי. מימוש DEF-007
נשמר ונדחף ב־`445a07a`; תיקון בידוד fixture הדפדפן נשמר ונדחף ב־`0ded7d0`.

```powershell
npx.cmd --no-install supabase test db supabase/tests/league-settings.test.sql supabase/tests/league-settings-concurrency.test.sql
```

```text
Files=2, Tests=131
All tests successful.
Result: PASS
```

`e2e/settings.spec.ts` הורץ כחלק מפקודת W3 המדויקת:

```powershell
npm.cmd run test:e2e:run -- e2e/scoring.spec.ts e2e/sync.spec.ts e2e/settings.spec.ts e2e/pagination.spec.ts
```

הריצה הראשונה הסתיימה ב־`7 passed, 1 failed`. שני מופעי settings עצמם עברו,
אך לאחר מופע ה־Desktop נשארה ליגה `completed` באותה עונת fixture; לכן תרחיש
scoring המאוחר ב־Pixel 5 קיבל את guard החוקי
`COMPLETED_RECONCILIATION_REQUIRED`, וה־cleanup שלו נכשל. נוסף `afterEach`
שמחזיר את ליגת settings ל־`open`, ואז הורץ:

```powershell
npx.cmd --no-install supabase db reset
npm.cmd run test:e2e:run -- e2e/scoring.spec.ts e2e/sync.spec.ts e2e/settings.spec.ts e2e/pagination.spec.ts
```

האיפוס המקומי החיל את כל ה־migrations עד
`20260826310000_slice9_keyset_pagination.sql` והסתיים בהצלחה. הריצה החוזרת
עברה `8 passed (1.1m)` — ארבעת ה־specs ב־Desktop Chrome וב־Pixel 5.

פקודות הסגירה על הראש הסופי עברו כך:

```text
npm.cmd run lint        -> PASS (exit 0)
npm.cmd run typecheck   -> PASS (exit 0)
npm.cmd run test        -> PASS; 40 files, 563 tests
npm.cmd run test:db     -> PASS; 19 files, 1182 tests
```

`npm.cmd run types:check` נכשל תחילה בגלל generated types stale. לאחר
`npm.cmd run types:db` (PASS), הרצה חוזרת של `npm.cmd run typecheck` עברה,
והרצה חוזרת של `npm.cmd run types:check` עברה עם
`Generated database types are current`. אין claim על תוצאה שלא הורצה כאן.
