# S9-REQ-003 — ראיית הגשה סופית ופרסום Public

סטטוס: `OWNER_ACTION_REQUIRED`.

## regression חדש

שם הבדיקה:
`owner-runbooks:check — S9-REQ-003 requires one exact final-production runbook and an empty sanitized template`.

לפני הוספת הארטיפקטים הורצה:

```powershell
npm.cmd run owner-runbooks:check
```

פלט שנצפה:

```text
Error: Missing owner runbook artifact:
docs/runbooks/slice-9-req-003-final-production-review.md
```

גם חוזה פנקס ההגשה הוחמר משבע פעולות owner היסטוריות לפעולה אחת:

```powershell
npm.cmd run submission:evidence:check
```

פלט ה־regression לפני עדכון הפנקס:

```text
Error: Final-submission evidence is missing: `<final-sha>`
```

## CI אמיתי שנצפה

```powershell
gh run view 33090719466 --attempt 2 --json databaseId,attempt,headSha,status,conclusion,url,jobs
gh run view 33090719466 --attempt 2 --log-failed
```

תוצאה: run `33090719466`, attempt 2, על
`e791f361444eb524099eddebdea1c92d6a3a0cc3` הסתיים `failure`. quality עבר
ב־1m24s, ‏DB עבר ב־3m46s, ו־Playwright נכשל ב־12m12s אחרי 28 tests בגלל:

```text
[WebServer] Error: The destination stream closed early.
28 passed (7.5m)
```

הכשל טופל ב־`223de65f083fcbf954c082c6e83c6df2ed14bdca` עם regression ייעודית
לסדר סגירת response streams. לאחר התיקון הורצו ונצפו:

```powershell
gh run view 33097585902 --json databaseId,attempt,headSha,status,conclusion,url,jobs
gh run view 33097590476 --json databaseId,attempt,headSha,status,conclusion,url,jobs
```

פלט מצונזר:

```text
33097585902 | head 223de65f... | success
  Lint, typecheck, unit tests and build | success | 1m28s
  Supabase database tests              | success | 4m35s
  Playwright core flows                | success | 13m22s
33097590476 | head 223de65f... | success
  Lint, typecheck, unit tests and build | success | 1m25s
  Supabase database tests              | success | 3m49s
  Playwright core flows                | success | 13m15s
```

Billing אינו עוד blocker. תוצאות אלו מוכיחות את התיקון, אך final-head CI עדיין
נדרש אחרי commit המסירה האחרון.

## runbook ותבנית

- `docs/runbooks/slice-9-req-003-final-production-review.md` מפרט מסך/פקודה
  מדויקים לכל שלב: SHA/PR/CI, ‏Hosted migrations, names/scopes, קידום
  Production, immutable+alias incognito, Demo-only, snapshot הגנות, מעבר
  ל־Public, גישה אנונימית ו־clean clone.
- `docs/evidence/slice-9/w8/S9-REQ-003-owner-template.md` נשאר כולו
  `NOT_RUN`/`NOT_CAPTURED` ואינו מכיל זהות, credentials או ערכי env.
- `docs/final-submission-evidence.md` משמר את הכשל האמיתי, שני ה־runs הירוקים
  ואת גבול הראיה, ודורש בדיוק owner row יחיד.

## מה שנותר לאחר merge

ה־runbook כולו executable בידי סוכן מאומת: final/main SHA ו־PR merge read,
CI, ‏Hosted migrations, ‏system actor readiness, ‏env names/scopes,
Production binding, ‏incognito Demo-only ושערי המסמכים. אין בו `env pull`,
קריאת Vault או הזנת secret מחדש.

אישור owner להעביר את המאגר ל־Public ולפרסם את ה־author metadata הטכני התקבל
ב־29.8.2026. אין עוד צורך בזהות GitHub של evaluator; שיטת Demo out-of-band,
אם נדרשת, אינה נכתבת ב־Git. לאחר merge הסוכן מבצע את יתר השלבים, מאמת גישה
אנונימית ו־clean clone ומתעד רק PASS וזמן. עד שה־Production הסופי קיים וכל
השדות מתייחסים לאותו SHA, הרשומה נשארת `OWNER_ACTION_REQUIRED`.
