# S9-REQ-003 — ראיית הגשה סופית וגישת evaluator

סטטוס: `OWNER_ACTION_REQUIRED`.

## regression חדש

שם הבדיקה:
`submission:evidence:check — current read-only facts cannot close owner-only final gates`.

לפני יצירת `docs/final-submission-evidence.md` הורצה:

```powershell
npm.cmd run submission:evidence:check
```

תוצאה שנצפתה:

```text
Error: Missing final-submission evidence register:
docs/final-submission-evidence.md
```

אחרי יצירת הפנקס:

```powershell
npm.cmd run submission:evidence:check
```

פלט שנצפה:

```text
Final-submission register verified: current read-only facts are separated from
seven owner-only final gates.
```

הבדיקה דורשת placeholders נפרדים ל־final candidate/Production, ‏Draft PR #14,
שני deployment IDs ו־immutable URLs שנצפו, annotation ה־Billing, מטריצת env,
migration head מקומי ושבע שורות OAR. היא נכשלת אם שער חיצוני מסומן
PASS/VERIFIED בלי ראיית owner או אם מופיע JWT/server-secret assignment.

## GitHub read-only

```powershell
git rev-parse HEAD
gh pr view 14 --json number,isDraft,state,headRefName,headRefOid,baseRefName,baseRefOid,url
gh pr checks 14
gh run view 33086758050
gh repo view talzantkeren/predictor --json url,visibility,nameWithOwner
```

פלט מצונזר שנצפה:

```text
HEAD=f1bf854e74233acb076152a8952c24373e1eec3a
PR #14: OPEN, isDraft=true
base main: 9fc73e425f0b9f9e0acb07e403ffdd3daaa2be0d
head: f1bf854e74233acb076152a8952c24373e1eec3a
repository visibility: PRIVATE
Vercel: pass, deployment dpl_Cjza13KogKyyLMbZr972AU5DmbY3
```

שלושת jobs של run `33086758050` לא התחילו. annotation שנצפה:

```text
The job was not started because recent account payments have failed or your
spending limit needs to be increased.
```

לכן אין CI PASS ל־checkpoint. `gh run view --log-failed` החזיר `log not found`
מפני שה־jobs לא התחילו; הוא אינו ראיית בדיקה.

## Vercel read-only

`vercel inspect --json` סונן לפני output לשדות deployment/alias/target/state
ול־timeout של `api/cron/sync` בלבד. לא נקרא או הודפס env value.

```text
Preview:
  id=dpl_Cjza13KogKyyLMbZr972AU5DmbY3
  immutable=predictor-mew7uo1y9-tals-projects-19902e47.vercel.app
  target/state=preview/READY
  alias=predictor-git-feature-slice-9-imp-51f991-tals-projects-19902e47.vercel.app
  cron timeout=60

Production:
  id=dpl_VtykjW3xjXJcjmpCjH25wZTBP3xn
  immutable=predictor-2r75rqica-tals-projects-19902e47.vercel.app
  target/state=production/READY
  alias=predictor-swart.vercel.app
  cron timeout=300
```

פקודת smoke אנונימית:

```powershell
curl.exe -L --max-time 20 -sS -o NUL -w '%{http_code} %{url_effective}' <url>
```

תוצאה שנצפתה: immutable+alias של Preview ו־Production החזירו 200. זה מוכיח
נגישות נוכחית ללא team login, אך לא final SHA. Preview timeout=60 גם אינו סוגר
את שער ה־120 שניות של Hosted Cron.

## מטריצת env ללא ערכים

```powershell
npx.cmd --no-install vercel env ls --json
```

הפלט סונן ל־`key/type/target/gitBranch`. נמצאו שמות production נדרשים,
entries היסטוריים של Slice 3/5, ו־:

```text
SPORTS_API_KEY | sensitive | preview,production | no branch
```

לכן S9-DEF-025 נשאר OAR. אין לבצע pull, reveal או צילום של value. המטריצה
המלאה והמצונזרת נשמרה ב־`docs/final-submission-evidence.md`.

## migrations מקומיות

```powershell
npx.cmd --no-install supabase migration list --local
```

תוצאה שנצפתה: 36 migrations מקומיות עד `20260827180000`. לא בוצעה פנייה
ל־linked Supabase בהתאם לגבול המשתמש; Hosted parity אינו נצפה.

## שערי מסמכים וקוד

```powershell
npm.cmd run submission:evidence:check
npm.cmd run docs:submission:check -- --online
npm.cmd run lint
npm.cmd run typecheck
git diff --check
```

תוצאות שנצפו:

```text
Final-submission register verified: ... seven owner-only final gates.
Submission docs verified: 13 documents, 34 local links, 5 external links checked online.
lint: exit 0
typecheck: exit 0
```

Production, Preview ו־Vercel docs החזירו 200; GitHub סווג
`PRIVATE_AUTH_REQUIRED`; localhost סווג `LOCAL_ONLY`. אין claim על evaluator
access, final deployment או Hosted migration parity.

## פעולות owner החסרות

1. להסדיר GitHub Billing/Spending limit ולהריץ מחדש את כל CI על
   `<final-candidate-sha>` עד שכל jobs באמת מתחילים ומסיימים בהצלחה.
2. אחרי S9-REQ-005 לרשום את ה־SHA הדחוף הסופי ולוודא PR #14 נשאר Draft; לא
   למזג, לא לאשר, לא להעביר ל־Ready ולא להפעיל auto-merge.
3. לפרוס את אותו SHA ב־Vercel בדרך owner מאושרת בלי merge; לקשר immutable URL,
   alias ו־Production ל־SHA המדויק ולאשר 200, Demo-only וללא protection.
4. להסיר Preview scope מ־`SPORTS_API_KEY` ולנקות entries היסטוריים רק אחרי
   בדיקת consumer. לצרף names/scopes בלבד ולפרוס מחדש; אין לחשוף value.
5. להשוות migration IDs של Hosted מול Git עד `20260827180000`, ולצרף parity
   מצונזר עם final deployment SHA. agent לא יפנה לפרויקט linked.
6. להעניק לזהות evaluator גישת read למאגר הפרטי וחשבונות Demo מחוץ ל־Git,
   לקבל אישור פתיחה, ולבצע incognito smoke מול immutable URL ו־alias הסופיים.
7. לרשום `<final-production-sha>` בפנקס רק אחרי שכל הראיות לעיל מתייחסות לאותו
   SHA. current READY/200 או תוצאות מקומיות אינם תחליף.

עד אז תנאי הקבלה של S9-REQ-003 אינם מתקיימים והרשומה אינה `VERIFIED`.
