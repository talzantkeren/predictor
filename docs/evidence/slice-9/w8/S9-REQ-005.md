# S9-REQ-005 — final hardening evidence

Status: `OWNER_ACTION_REQUIRED`

Implementation candidate: `2a692242c795b3129792b7b7f7cd203c1776f9f9`
(pushed to `origin/feature/slice-9-implementation`). The subsequent ledger/evidence
commit does not change runtime code. Evidence was observed locally on 27 August
2026. No Hosted, Production, provider-live, native-zoom or human-rehearsal result
is inferred from these commands.

## Regression that fails without the fix

Command:

```powershell
npm.cmd run hardening:check
```

Test: `scripts/check-final-hardening.mjs`

Observed before `docs/final-hardening-register.md` existed (sanitized excerpt):

```text
exit=1
Error: Missing final hardening register: docs/final-hardening-register.md
```

Observed after adding the register and owner-gate contract:

```text
exit=0
Final hardening contract verified: local gates, scale/UI regressions,
accepted-risk fields and ten owner-only gates are explicit.
```

The checker also requires all five viewports, four representative scale plans,
the local command matrix, P0/P1/P2 disposition fields, owner/date/trigger fields,
and rejects secret-like assignments or JWT-like text.

## Install and full local matrix

Command:

```powershell
npm.cmd ci
```

Observed excerpt:

```text
added 425 packages, and audited 426 packages in 29s
found 0 vulnerabilities
```

Command:

```powershell
npm.cmd run verify
```

Observed tests and excerpts:

```text
eslint ...                                      PASS (exit 0)
tsc --noEmit                                    PASS (exit 0)
Test Files 48 passed (48); Tests 627 passed
Files=30, Tests=1443; Result: PASS
Generated database types are current.
Client secret scan passed across 52 build artifacts.
28 passed (6.0m)
```

The 30 database files include exact-authorization negative tests, scoring and
state-transition tests, and the real dblink multi-session concurrency files:
`keyset-pagination-concurrency`, `league-settings-concurrency`,
`manual-match-concurrency`, `manual-override-clear-concurrency`,
`slice9-lifecycle-concurrency`, and `slice9-review-concurrency`. Playwright
included the two-user isolation/IDOR flows, exact-resource pagination,
open→active/current→completed/final lifecycle, corrected scoring, and Desktop +
Mobile projects. No `[WebServer] Error` appeared; the only server output was the
sanitized `NO_COLOR`/`FORCE_COLOR` warning.

## Separate build, client scan, audit and database gates

Commands:

```powershell
npm.cmd run build
$env:CLIENT_SECRET_SENTINEL='slice9-final-client-secret-sentinel-never-ship-20260827'; npm.cmd run test:client-secrets
npm.cmd audit --audit-level=low
npx.cmd --no-install supabase db lint --local --schema public,private --level warning --fail-on error
npx.cmd --no-install supabase db reset --local
npm.cmd run types:check
```

Observed excerpts:

```text
Next.js 16.3.0 ... Compiled successfully; exit 0
Client secret scan passed across 52 build artifacts.
found 0 vulnerabilities
Linting schema: public / private; No schema errors found; results=[]
Applied 36 forward migrations through 20260827180000; reset finished
Generated database types are current.
```

Only the local reset was executed. No linked or Hosted reset was run.

## Representative hundreds-user scale plans

Command:

```powershell
npm.cmd run scale:plans
```

Test: `scripts/check-scale-plans.ts`, using the real rollback-only pagination
fixture with 202 auth users, 101 leagues and 202 prediction rows. It runs
`EXPLAIN (ANALYZE, BUFFERS)` against the four production RPC signatures and
fails at 500ms or if a result exceeds one bounded page.

Observed after the forward-migration reset:

```text
Representative scale plans passed:
dashboard_leagues_101, eligible_leagues_101,
revealed_predictions_202, active_members_201;
execution=0.153ms, 2.182ms, 0.464ms, 0.834ms;
rows=51,51,51,26.
```

The complete plan output stayed untracked under `tmp/final-hardening/`; it
contained no provider payload or secret.

## Viewport, keyboard, contrast and touch evidence

Command:

```powershell
$env:FINAL_VIEWPORT_AUDIT='true'; npm.cmd run test:e2e:run -- e2e/home.spec.ts --project=desktop-chromium
```

Test: `home smoke › @preview loads a stable RTL Demo home page`.

Observed excerpt:

```text
1 passed (2.7s)
```

The regression exercised 360×800, 390×844, 768×1024, 1024×768 and 1440×900,
and asserted RTL, no horizontal overflow, WCAG contrast ≥4.5 for Demo status,
44×44 minimum auth targets, and visible keyboard focus. All five fresh PNGs were
visually inspected: no clipping, overlap, truncated text or layout break was
observed. Native Chrome 200% is deliberately not simulated and remains an owner
action.

## Three clean S9-DEF-024 repeats

Exact command executed three times sequentially:

```powershell
npm.cmd run test:e2e:run -- e2e/prediction-lock.spec.ts
```

Test: `matches, predictions, lock, and reveal › enforces the complete two-member
flow with database time`, Desktop Chromium + Mobile Chromium.

Observed excerpts:

```text
repeat 1: 2 passed (26.6s)
repeat 2: 2 passed (26.6s)
repeat 3: 2 passed (26.8s)
```

No repeat emitted `[WebServer] Error`; only the sanitized color-environment
warning appeared.

## Clean-clone reproducibility

The target was a new path beneath the resolved Windows temp directory. No
existing directory was overwritten. The ignored `.env.local` was copied without
printing it; `git status --short` was empty both before and after the checks.

Commands from the clone at exact SHA
`2a692242c795b3129792b7b7f7cd203c1776f9f9`:

```powershell
npm.cmd ci
npm.cmd run verify
npm.cmd run build
git status --short
```

Observed excerpts:

```text
added 425 packages ... in 21s; found 0 vulnerabilities
Test Files 48 passed (48); Tests 627 passed (627)
Files=30, Tests=1443; Result: PASS
Generated database types are current.
Client secret scan passed across 52 build artifacts.
28 passed (5.8m)
standalone build: Compiled successfully; exit 0
git status --short: empty
```

No `[WebServer] Error` appeared in the clean-clone run.

## Required owner actions and residual risks

The local and clean-clone gates do not close the following external/human
acceptance criteria. Exact owner, target date and trigger are recorded in
`docs/final-hardening-register.md`.

| Gate | Status | Exact missing action |
| --- | --- | --- |
| Hosted password policy | OWNER_ACTION_REQUIRED | Read-only verify Production min/max is 8–128 and attach sanitized rate-limit/monitoring evidence; do not claim leaked-password protection is enabled. |
| Hosted Advisors | OWNER_ACTION_REQUIRED | Export sanitized current Security and Performance Advisor lists on the final Production SHA and record a disposition for every item; any accepted risk needs owner/date/trigger and approval. |
| Native 200% zoom | OWNER_ACTION_REQUIRED | Run native Chrome 200% keyboard/layout checks on the final Preview SHA and attach sanitized screenshots/results. |
| GitHub CI billing | OWNER_ACTION_REQUIRED | Resolve the repository Billing/Spending-limit block, run unchanged CI on the candidate, and attach the green run URL. |
| Vercel secret scope | OWNER_ACTION_REQUIRED | Remove `SPORTS_API_KEY` from Preview without reading it, keep Sensitive Production-only, redeploy, and attach names/types/scopes plus clean Preview scan. |
| Production Cron | OWNER_ACTION_REQUIRED | After the final deploy, attach one sanitized Cron response tied to a single finalized run and released lease. |
| Hosted migration parity | OWNER_ACTION_REQUIRED | Compare read-only Hosted/local migration versions through `20260827180000`; do not reset Hosted. |
| Evaluator access | OWNER_ACTION_REQUIRED | Verify approved evaluator access to the private repository and Production URL in an incognito session. |
| Human rehearsal | OWNER_ACTION_REQUIRED | Perform and log a 10–15 minute rehearsal on the candidate, including fallback/outage. |
| Final Production SHA | OWNER_ACTION_REQUIRED | Deploy and record exact immutable/alias SHA parity after CI and owner gates complete. |

There are no known local P0/P1 failures and no P2 waiver. S9-REQ-005 remains P2
and `OWNER_ACTION_REQUIRED`; historical Advisor counts are not item-level closure
evidence. The leaked-password-protection limitation remains the explicitly
accepted residual risk under S9-TDEC-004, subject to the documented compensating
controls and owner verification.

