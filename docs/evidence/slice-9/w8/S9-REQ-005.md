# S9-REQ-005 — final hardening evidence

Status: `OWNER_ACTION_REQUIRED`

Implementation checkpoint:
`a0610df0c0dad8634cbf512f9ff0b74b1bde728b`. All commands below used Local
Supabase only. No Hosted setting was changed, no linked reset was run, and no
secret value or provider payload was printed or committed.

## Regression and real failure

The rewritten checker is `scripts/check-final-hardening.mjs`. It requires all
local dispositions, five widths, four representative plan labels, risk fields,
exactly one owner-action row, and separate-record routing for every other gate.

Before the register was rewritten:

```text
$ npm.cmd run hardening:check
Error: Final hardening register is missing: single remaining owner action
EXPECTED_FAILURE_EXIT=1
```

Afterward:

```text
$ npm.cmd run hardening:check
Final hardening contract verified: local gates and dispositions are explicit;
exactly one Hosted read/export owner action remains.
```

The first full verification also failed for a real reason; it was not relabeled
or bypassed:

```text
$ npm.cmd run verify
Test Files 1 failed | 48 passed (49)
Tests 1 failed | 630 passed (631)
playwright-response-streams.test.ts:
accessibility-matrix.spec.ts imported Page from @playwright/test directly
```

The accessibility spec now imports `type Page` from
`e2e/support/stream-safe-test.ts`, keeping every spec behind the automatic
response-stream fixture. Focused proof:

```text
$ npx.cmd vitest run src/lib/playwright-response-streams.test.ts
Test Files 1 passed (1)
Tests 4 passed (4)

$ npm.cmd run lint
PASS

$ npm.cmd run typecheck
PASS
```

## Clean install and full verify

Commands:

```powershell
npm.cmd ci
npm.cmd run verify
```

Observed sanitized result:

```text
427 packages added; 428 audited; 0 vulnerabilities
lint: PASS
strict typecheck: PASS
Vitest: 49 files, 631/631
pgTAP: 30 files, 1443/1443, 28s
Generated database types are current.
Build: PASS
Client secret scan: 52 artifacts
Playwright: 38/38 in 5.9m
```

The 38 browser tests include 10 accessibility-matrix cases across both browser
projects, exact-authorization negatives, the complete lifecycle, scoring,
pagination and Manual Sports sync. No `[WebServer] Error` appeared.

## Standalone local gates

Commands:

```powershell
npm.cmd run build
$env:CLIENT_SECRET_SENTINEL='<synthetic>'; npm.cmd run test:client-secrets
npm.cmd audit --audit-level=low
npx.cmd --no-install supabase db lint --local --schema public,private --level warning --fail-on error
npx.cmd --no-install supabase db reset --local
npm.cmd run types:check
npm.cmd run scale:plans
```

Observed sanitized result:

```text
Next.js 16.3.0: compiled; TypeScript/page-data/static generation completed
Client secret scan passed across 52 build artifacts.
Dependency audit: 0 vulnerabilities
DB lint: public/private; results=[]; no schema errors
Forward reset: 36 migrations through 20260827180000; seed/restart completed
Generated database types are current.
Scale plans: dashboard_leagues_101=0.163ms/51 rows;
eligible_leagues_101=2.173ms/51;
revealed_predictions_202=0.456ms/51;
active_members_201=0.830ms/26
```

## Accessibility and clean E2E repeats

Test name:
`automated accessibility viewport matrix > <width>px: axe, keyboard, focus,
contrast and touch contracts`.

It passed as part of the full 38-test run at 360/390/768/1024/1440 in Desktop
and Mobile Chromium. The automated portion covers axe A/AA, keyboard order,
visible focus, contrast, 44px non-inline targets, RTL, overflow and reduced
motion. Native 200% belongs solely to `S9-DEF-022`.

Exact repeat command, run three times sequentially:

```powershell
npm.cmd run test:e2e:run -- e2e/prediction-lock.spec.ts
```

Observed:

```text
repeat 1: 2/2 passed (27.3s)
repeat 2: 2/2 passed (27.1s)
repeat 3: 2/2 passed (27.1s)
```

No repeat emitted `[WebServer] Error`.

## Exact-SHA clean clone

A new clone was created only under ignored workspace `tmp/`, checked out at
`a0610df0c0dad8634cbf512f9ff0b74b1bde728b`, and received the ignored local
environment without printing it. Commands inside the clone:

```powershell
npm.cmd ci
npm.cmd run verify
npm.cmd run build
git status --short
```

Observed:

```text
CLEAN_CLONE_SHA=a0610df0c0dad8634cbf512f9ff0b74b1bde728b
427 packages added; 428 audited; 0 vulnerabilities
Vitest 631/631; pgTAP 1443/1443; types current
client scan 52; Playwright 38/38 in 6.1m
standalone production build: PASS
CLEAN_CLONE_STATUS=clean
```

The copied `.env.local` was removed after the run and remains untracked. The
temporary dependency tree contains no evidence artifact and is ignored.

## Disposition

- P0: zero known local findings; no waiver.
- P1: zero known local findings; no waiver.
- P2: this record remains open and is not waived.
- Local install/verify/build/audit/DB/reset/types/scale/accessibility/repeats/
  clean-clone items are closed with observed results in
  `docs/final-hardening-register.md`.
- Native zoom, Vercel scope, Cron, rehearsal, Hosted parity/evaluator/final SHA
  are explicitly routed to their own records and are not duplicate actions here.

## The one remaining owner action

In one read-only Supabase Production sitting, the Project owner must:

1. capture the Hosted password policy showing the 8–128 contract plus relevant
   rate-limit/monitoring settings, without changing configuration and without
   claiming leaked-password protection is enabled; and
2. export sanitized Security Advisor and Performance Advisor lists on the final
   Production SHA, assigning every item `fixed`, `informational/no-action` with
   rationale, or `accepted risk` with Owner/Target date/Trigger and approval.

Save the artifacts in the owner packet paths named by
`docs/slice-9-owner-actions.md`, then run `npm.cmd run hardening:check`. This is
one combined owner action; there is no remaining agent-doable work for this
record.
