# S9-DEF-015 — ראיית סגירה

## תוצאה

`VERIFIED` מקומית ב־27 באוגוסט 2026.

- `containsDangerousBidiControl` דוחה את כל U+061C, ‏U+200E/U+200F,
  U+202A–U+202E ו־U+2066–U+2069 בגבולות display name, שדות ליגה, סיבת דחייה
  ונרמול team/round של API-Football. הודעת provider נשארת generic.
- המיגרציה forward-only
  `20260827180000_slice9_bidi_text_hardening.sql` מוסיפה 11 check constraints
  validated ל־profiles/leagues/join_requests/catalog/provider labels. אין
  טבלה, policy, grant או function חדשים. נתון legacy מסוכן אינו נמחק או משתנה
  בשקט; המיגרציה נכשלת סגור אם קיים row כזה.
- `IsolatedText` מפיק `<bdi dir="auto">` ומשמש בשמות סביב status, rank, score,
  cards ו־headings. `option` שאינו יכול להכיל `bdi` מקבל `dir="auto"`.
- שמות ארוכים וטקסט עברי/ערבי/Latin מעורב רגיל נשארים מותרים.

## רגרסיות חדשות

- `src/lib/untrusted-text.test.ts` — כל 12 תווי הכיווניות + mixed scripts.
- `src/components/ui/isolated-text.test.ts` — DOM מדויק של boundary משותף.
- `auth-rules`, ‏`leagues-rules`, ‏`membership-rules` ו־
  `api-football-provider` — RLO/LRE/RLI/PDI ודחיית PII/provider label בלי דליפה.
- `supabase/tests/slice9-bidi-hardening.test.sql` — 11 constraints, ‏mixed text
  תקין ודחייה בכל עמודה, כולל ארבעת מקרי הכיווניות המחייבים.
- `e2e/leagues.spec.ts` — RLO נדחה דרך המוצר; שם mixed תקין מבודד בסיכום
  וב־Dashboard בשני viewports.

ללא התיקון, בדיקות Zod/provider מקבלות controls, ה־DB updates מצליחים,
`bdi[dir="auto"]` חסר וה־Playwright יוצר שם עם RLO במקום להישאר בטופס.

## פקודות ופלט שנצפו

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npx.cmd --no-install supabase db reset --local
```

```text
Applying migration 20260827180000_slice9_bidi_text_hardening.sql...
Finished supabase db reset on branch feature/slice-9-implementation.
```

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npx.cmd --no-install supabase test db supabase/tests/slice9-bidi-hardening.test.sql
```

```text
Files=1, Tests=16
Result: PASS
```

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd run types:db; npm.cmd run types:check
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd test -- src/components/ui/isolated-text.test.ts src/lib/untrusted-text.test.ts src/features/auth/auth-rules.test.ts src/features/leagues/leagues-rules.test.ts src/features/membership/membership-rules.test.ts src/features/sports/api-football-provider.test.ts
```

```text
Generated database types are current.
Test Files  6 passed (6)
Tests       193 passed (193)
```

בדיקת ה־DOM נכתבה תחילה כ־`.test.tsx`; ‏Vitest config כולל בכוונה רק
`.test.ts`, ולכן הריצה הראשונה מנתה 5 קבצים בלבד ולא נטענה כראיה. הבדיקה
הומרה ל־`.test.ts` בלי לשנות/להחליש את config, והריצה החוזרת לעיל מנתה 6/6.

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd run test:e2e -- e2e/leagues.spec.ts
```

הריצה הראשונה נכשלה ב־build typecheck משום ש־`reviewCode` nullable לא צומצם
לפני `IsolatedText`; לא הורץ דפדפן ולא נטען PASS. לאחר הוספת guard מפורש,
אותה פקודה עברה:

```text
build: PASS — Compiled successfully
Client secret scan passed across 52 build artifacts.
Running 2 tests using 1 worker
2 passed (13.7s) — desktop-chromium + mobile-chromium
```

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd test
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd run test:db
```

```text
Vitest: Test Files 44 passed (44); Tests 617 passed (617)
pgTAP final: Files=30, Tests=1443; Result: PASS
```

ריצת ה־DB המלאה הראשונה נכשלה 1/1443: ה־exact constraint register של
`membership.test.sql` זיהה בצדק constraint חדש שלא הופיע ברשימה הצפויה.
לא נחלשה הבדיקה; הרשימה עודכנה בשם המדויק. לאחר מכן:

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npx.cmd --no-install supabase test db supabase/tests/membership.test.sql supabase/tests/slice9-bidi-hardening.test.sql; if ($LASTEXITCODE -eq 0) { npm.cmd run test:db }
```

```text
focused: Files=2, Tests=114; Result: PASS
full: Files=30, Tests=1443; Result: PASS
```

```powershell
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npm.cmd run lint; npm.cmd run typecheck; npm.cmd run types:check
$env:PATH='C:\Users\Tal\AppData\Local\nvm\v24.16.0;' + $env:PATH; npx.cmd --no-install supabase db lint --local
git diff --check
```

```text
lint: PASS — exit 0
typecheck: PASS — exit 0
types:check: Generated database types are current.
db lint: exit 0; no product-schema issue under public/private. Existing pgTAP extension diagnostics remain under extensions.
git diff --check: exit 0; only local line-ending warnings.
```

לא בוצעה קריאת provider חיה, לא הופעל Supabase linked/Hosted ולא נרשם payload
או control מסוכן בראיה.
