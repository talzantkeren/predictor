# S9-REQ-002 — מצגת, תסריט וחזרה

סטטוס: `OWNER_ACTION_REQUIRED`.

## תוצרים שנבנו ונבדקו

- `presentation/predictor1-final-project.pptx`: מצגת 16:9 עריכה בת 9 שקפים.
- `presentation/predictor1-final-project/slide-1.png` עד `slide-9.png`: render
  סופי. כל תשעת השקפים נבדקו חזותית בגודל המקורי; לא נמצאו חיתוך, overflow,
  חפיפה או סדר קריאה שבור לאחר תיקון שקף הסיום.
- לכל שקף speaker notes עם תזמון ובלוק `[Sources]`; שקף 9 כולל hyperlinks
  מדויקים ל־Production ול־GitHub.
- `presentation/demo-script.md`: מסלול מתוזמן 00:00–11:30, checklist, שאלות
  evaluator ונוהל outage.
- `presentation/fallback/`: שלושה צילומי מוצר מצונזרים ממצבי
  open, active/current ו־completed/final.
- `presentation/rehearsal-log.md`: שער אנושי שאינו מאפשר PASS אוטומטי.

## regression חדש

שם הבדיקה: `presentation:check — editable deck, notes, links, renders, fallback and runbook`.

פקודה:

```powershell
npm.cmd run presentation:check
```

פלט מצונזר שנצפה:

```text
Presentation artifacts verified: 9 editable/rendered slides, 3 fallback images,
notes, public links and presenter runbook.
```

הוכחת negative: הקובץ `03-completed-final-report.png` הועבר זמנית לנתיב ignored,
אותה בדיקה הורצה, והקובץ הוחזר ב־`finally`. תוצאה שנצפתה:

```text
Error: Missing required presentation artifact:
presentation/fallback/03-completed-final-report.png
EXPECTED_FAILURE_EXIT=1
```

אחרי ההחזרה אותה פקודה עברה. הבדיקה גם פותחת את מבנה ZIP/OOXML של ה־PPTX,
סופרת 9 slides ו־9 notes, מאמתת `[Sources]`, שני קישורים, media מוטמע וחתימות
וממדי PNG. לכן היא הייתה נכשלת ללא תוצרי התיקון ואינה נשענת על baseline ירוק.

## לכידת fallback דרך התרחיש המוצרי

פקודה:

```powershell
$env:CAPTURE_PRESENTATION_ASSETS='true'
npm.cmd run test:e2e:run -- e2e/lifecycle.spec.ts --project=desktop-chromium
```

שם התרחיש:
`runs open to active/current to completed/final with frozen reconciliation`.

פלט מצונזר שנצפה:

```text
1 passed (58.4s)
```

הלכידה opt-in, פעילה רק ב־desktop, משתמשת בשמות קבועים וממסכת שמות דינמיים.
היא מתבצעת רק לאחר שה־UI הציג כל מצב. אין בצילומים דוא״ל, סיסמה, invite
fragment, proof path או signed URL. התרחיש הרגיל אינו כותב קבצים כשהדגל חסר.

## render ופריסה

פקודות (עם runtime paths מקומיים שסופקו לסביבת המצגות):

```powershell
python render_slides.py presentation/predictor1-final-project.pptx \
  --output_dir presentation/predictor1-final-project
python slides_test.py presentation/predictor1-final-project.pptx
```

פלט שנצפה:

```text
Slides rendered to ...\presentation\predictor1-final-project
Test passed. No overflow detected.
```

הבדיקה החזותית הידנית כיסתה בנפרד `slide-1.png` עד `slide-9.png` אחרי ה־render
האחרון. התמונות מייצגות את ה־PPTX הסופי ולא render ביניים.

## שערי repository

```powershell
npm.cmd run lint
npm.cmd run typecheck
git diff --check
```

תוצאות שנצפו: `lint` עבר עם exit 0; ‏`typecheck` עבר עם exit 0;
`git diff --check` לא מצא שגיאת whitespace. בנוסף, ריצת Vitest המלאה שנצפתה
באותו session עברה `48 files, 627/627 tests`.

## קישורים

```powershell
curl.exe -L --max-time 20 -sS -o NUL -w '%{http_code} %{url_effective}' <url>
gh repo view talzantkeren/predictor --json url,visibility,nameWithOwner
git remote get-url origin
```

תוצאה שנצפתה:

```text
200 https://predictor-swart.vercel.app/
200 https://predictor-git-feature-slice-9-imp-51f991-tals-projects-19902e47.vercel.app/
404 https://github.com/talzantkeren/predictor
{"nameWithOwner":"talzantkeren/predictor","url":"https://github.com/talzantkeren/predictor","visibility":"PRIVATE"}
https://github.com/talzantkeren/predictor.git
```

ה־GitHub URL מדויק ונפתח עם זהות owner מאומתת; 404 אנונימי צפוי משום שהמאגר
פרטי. אין claim של evaluator access כאן. מסירת גישת evaluator שייכת גם לשער
S9-REQ-003 ונשארת פעולה אנושית.

## פעולת owner החסרה

ה־owner חייב, על אותו candidate SHA שמיועד להגשה:

1. להריץ `git pull` ו־`git rev-parse HEAD` ולרשום את ה־SHA ב־
   `presentation/rehearsal-log.md`.
2. להשתמש בשני חשבונות Demo מורשים שנמסרו מחוץ ל־Git, להפעיל טיימר ולבצע בקול
   וב־UI את כל `presentation/demo-script.md`; אין mutation ישיר למסד.
3. לתעד משך אמיתי בין 10:00 ל־15:00 דקות, קריאות של 9/9 שקפים, פתיחת קישורי
   Production ו־GitHub בזהות המציג/ה, והיכולת להסביר components, libraries,
   security, scale, tradeoffs וגבולות Demo.
4. לדמות outage, לעבור בתוך 20 שניות לשלושת קובצי fallback ולסיים בלי claim על
   פעולה שלא נצפתה.
5. למלא תוצאה מצונזרת ביומן. אם סעיף נכשל, להשאיר FAIL/OAR ולציין תיקון מדויק.

עד לפעולה זו תנאי הקבלה של חזרה מלאה 10–15 דקות אינו נצפה, ולכן הרשומה אינה
`VERIFIED`.

## Final closeout — deck, timing and evaluator packet

ה־PPTX העריך נבדק מחדש עם Artifact Tool: ‏9 slides, ‏9 note records ו־9
renders. בבדיקת full-size של כל שקף נמצא ששקף 7 עדיין הציג totals ותאריך
מיושנים. אותו שקף נערך במקום בלבד: הכותרות היציבות `RULES`, ‏`DATA`, ‏`FLOWS`
ותיאור coverage החליפו totals/file-counts, בלי שינוי layout, media, links,
notes, master או theme. ‏Theme SHA נשמר זהה, לא נמצאו slide placeholders
ריקים, fidelity דיווח 0 issues ו־overflow test עבר.

נוספו ונקשרו לחבילת המצגת:

- `presentation/deck-source.md` — manifest אנושי של כל 9 השקפים והמקורות;
- `presentation/timing-guide.md` — יעד 11:30, טווח 10:00–15:00, cut rules,
  hard stop ו־outage אחרי 20 שניות;
- `presentation/evaluator-checklist.md` — מוצר, ארכיטקטורה, אבטחה, scale,
  גבולות Demo, קישורים ושורת ראיה ריקה;
- `presentation/demo-script.md`, ‏`README.md` ו־`rehearsal-log.md` עודכנו
  כך שישתמשו בשלושת התוצרים ובאותו score sequence דטרמיניסטי.

הרגרסיה הורחבה כך שתיכשל אם אחד משלושת הקבצים חסר או אם תוכן שקף 7 חוזר
ל־totals/date הישנים. ההרצה השלילית שנצפתה לפני הוספת ה־manifest:

```text
$ npm.cmd run presentation:check
Error: Missing required presentation artifact: presentation/deck-source.md
EXPECTED_FAILURE_EXIT=1
```

לאחר הסגירה:

```text
$ npm.cmd run presentation:check
Presentation artifacts verified: 9 editable/rendered slides, 3 fallback images,
notes, deck source, timing guide, evaluator checklist and public links.

$ python slides_test.py presentation/predictor1-final-project.pptx
Test passed. No overflow detected.

$ node check_template_fidelity.mjs <sanitized local paths>
status=pass; issueCount=0

$ Artifact Tool import/render/inspect
Artifact Tool inspection complete: 9 slides, 9 note records.
Edited and rendered 9 slides with Artifact Tool.

$ PPTX package checks (in memory)
THEME_SHA_MATCH=True
SLIDE_PLACEHOLDERS=0
EMPTY_SLIDE_PLACEHOLDERS=0

$ curl.exe -L --max-time 20 <Production and Preview>; gh repo view ...
Production=200; Preview=200; repository visibility=PRIVATE (authorized gh view)
```

שלושת צילומי ה־fallback נפתחו ונבדקו בנפרד בגודל המקורי: open,
active/current ו־completed/final; לא נמצא בהם secret, email, invite fragment,
proof path או signed URL. אין claim שחזרה אנושית בוצעה.

### הפעולה היחידה שנותרה לבעלים

לבצע **חזרה אנושית מתוזמנת אחת ורציפה** על ה־candidate SHA: לפתוח את ה־PPTX,
Production, GitHub ושני חשבונות ה־Demo שנמסרו מחוץ ל־Git; להריץ בקול וב־UI את
`demo-script.md` לפי `timing-guide.md`, לרבות outage handoff; להשלים את
`evaluator-checklist.md` ואת שורת `rehearsal-log.md`. PASS מותר רק במשך
10:00–15:00, ‏9/9 שקפים, שני קישורים והסברים מלאים. זו פעולה אנושית אחת;
אין פעולה נוספת בצד agent לרשומה.
