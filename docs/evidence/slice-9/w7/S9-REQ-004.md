# S9-REQ-004 — חבילת מסמכי הגשה מסונכרנת

סטטוס: `VERIFIED`.

## שינוי ותיחום

- README אינו מציג עוד את Slice 8 כמצב הנוכחי או את lifecycle כעבודה עתידית.
- `docs/testing.md`, ‏`docs/security.md` ו־`docs/scale.md` כוללים snapshot טרי,
  גבולות lifecycle, capacity ופעולות owner שיוריות בלי להפוך אותן ל־PASS.
- `docs/evaluator-runbook.md` מרכז קישורים, clean clone, Demo path, access
  boundaries ופעולות owner בלי credential.
- `docs/project-book-source.md` עודכן לגרסה 1.3 ולספירות שנמדדו מחדש;
  `docs/project-book.docx` נוצר מחדש דטרמיניסטית.
- אין migration, שינוי RLS/grant, ספק חי, linked mutation או שינוי היקף מוצר.

## regression חדש למסמכי ההגשה

שם הבדיקה:
`docs:submission:check — synchronized facts, local/external links, clean clone and safe owner gates`.

לפני הוספת snapshot המסירה ל־`docs/testing.md` הורצה:

```powershell
npm.cmd run docs:submission:check
```

פלט שנצפה:

```text
Error: Testing snapshot is missing: ## Snapshot מסירה — S9-REQ-004
```

אחרי התיקון:

```powershell
npm.cmd run docs:submission:check
npm.cmd run docs:submission:check -- --online
```

פלט מצונזר שנצפה:

```text
Submission docs verified: 12 documents, 33 local links, 5 external links inventoried.
Submission docs verified: 12 documents, 33 local links, 5 external links checked online.
LOCAL_ONLY http://localhost:54324
PRIVATE_AUTH_REQUIRED https://github.com/talzantkeren/predictor
200 https://predictor-git-feature-slice-9-imp-51f991-tals-projects-19902e47.vercel.app
200 https://predictor-swart.vercel.app
200 https://vercel.com/docs/functions/configuring-functions/duration
```

404 אנונימי של GitHub מסווג במפורש כ־private access required; הקישור המדויק
אומת גם מול `origin`. גישת evaluator אינה נטענת כ־PASS ונשארת ב־S9-REQ-003.

הבדיקה גם דוחה מצב Slice 8 stale, lifecycle עתידי, Slice 10, טענת דוח פיננסי,
טענת מוכנות לא־מאושרת, JWT/server-secret assignment, manifest מקור קורס שסטה,
PDF קורס ב־root, קישור פנימי שבור או runbook בלי clean-clone/owner gates.

## ספירות טריות

```powershell
npm.cmd run test
npm.cmd run test:db
npm.cmd run test:e2e:run
```

תוצאות שנצפו:

```text
Test Files  48 passed (48)
Tests       627 passed (627)

All tests successful.
Files=30, Tests=1443
Result: PASS

28 passed (5.7m)
```

ריצת Playwright הראשונה סיימה 28/28 assertions אך נכשלה בצדק על:

```text
[WebServer] Error: The destination stream closed early.
Playwright emitted an unexpected local web-server error; the passing test count
is not sufficient evidence.
```

ה־lifecycle test המתין לסיום שני RSC response streams לפני teardown. לאחר
התיקון `e2e/lifecycle.spec.ts` עבר 2/2 ב־Desktop/Mobile, והמטריצה המלאה החוזרת
עברה 28/28 בלי `[WebServer] Error`. רק הריצה החוזרת משמשת ראיית PASS.

## ספר הפרויקט

פקודות:

```powershell
npm.cmd run docs:book
npm.cmd run docs:book:check
npx.cmd vitest run src/lib/project-book-contract.test.ts
```

פלט שנצפה:

```text
Generated docs\project-book.docx from docs\project-book-source.md
Generated project book is current and deterministic.
Test Files  1 passed (1)
Tests       3 passed (3)
```

`render_docx.py` נכשל מפני ש־LibreOffice לא נמצא (`WinError 2`). לפי ה־workflow
הקנוני בוצע fallback: Word פתח את ה־DOCX read-only, ייצא PDF, ו־Poppler
`pdftoppm -png -r 144` הפיק `page-1.png` עד `page-5.png`.

ב־render הראשון עמוד 1 חשף footer ישן `v1.2` מול כותרת 1.3. הגנרטור תוקן,
הספר נוצר מחדש, drift check וה־contract עברו שוב, ו־render שני הפיק 5 עמודים.
כל 5/5 העמודים נפתחו בנפרד ב־100%: RTL, טבלאות, header/footer, קישורים ומעברי
עמוד נקיים; לא נמצאו clipping, overlap, glyph חסר או עמוד כמעט ריק.

## שערי build ואבטחת client

```powershell
npm.cmd run lint
npm.cmd run typecheck
npm.cmd run build
$env:CLIENT_SECRET_SENTINEL='slice9-req004-synthetic-client-scan-sentinel-27aug2026'
npm.cmd run test:client-secrets
npm.cmd run presentation:check
git diff --check
```

תוצאות שנצפו:

```text
lint: exit 0
typecheck: exit 0
Compiled successfully
Client secret scan passed across 52 build artifacts.
Presentation artifacts verified: 9 editable/rendered slides, 3 fallback images,
notes, public links and presenter runbook.
```

ריצת client scan ראשונה ללא sentinel נכשלה ב־
`CLIENT_SECRET_SENTINEL is required`; היא אינה נספרת כ־PASS. הריצה החוזרת
השתמשה בערך סינתטי, לא ב־secret, ועברה. `git diff --check` לא מצא שגיאת
whitespace. כל הספירות והקישורים במסמכים תואמים לתוצאות שנצפו לעיל.

## סגירת ביקורת עצמאית — enforcement של מסמכי ההגשה

ב־28.8.2026 `docs:book:check` ו־`docs:submission:check` חוברו במפורש גם ל־CI
וגם ל־`npm run verify`. שער הספר מבצע regeneration והשוואת bytes אמיתית;
שער המסירה נשאר offline ואינו מציג inventory של קישורים חיצוניים כאימות רשת.

```text
npm run docs:book:check
npm run docs:submission:check

Book: current and deterministic, exit 0.
Submission docs: 15 documents, 34 local links, 4 external links inventoried,
exit 0.
```

לא בוצע `--online`, לא נטען URL חיצוני ולא נוספה טענת Hosted חדשה.

## סגירת ביקורת עצמאית — נתיבי מפתח מקומיים

כל נתיבי ה־home המוחלטים שנמצאו בפלטי evidence הוחלפו ב־`<repo>` או ב־
placeholder מפורש של runtime מקומי. התוכן הסמנטי, הפקודה ותוצאת הבדיקה נשמרו;
רק מיקום פרופיל המפתח הוסר. `repository-path-hygiene.test.ts` סורק את כל קובצי
הטקסט tracked דרך `git ls-files`, מדלג על binary לפי NUL ומדווח `file:line`
עבור נתיבי macOS/Linux/root/workspace/WSL ו־Windows user-profile. גם דוגמאות
ה־regex מורכבות בזמן ריצה, ולכן קובץ ה־guard עצמו אינו מוחרג מן הסריקה.

```text
npm run test -- src/lib/repository-path-hygiene.test.ts
npm run test -- src/lib/repository-path-hygiene.test.ts
npm run test -- src/lib/repository-path-hygiene.test.ts
npm run lint

Focused run before staging the new guard: 1 file, 6 tests, PASS.
Focused run after staging the guard so it scans its own future tracked source:
1 file, 6 tests, PASS.
Focused run after staging all sanitized evidence: 1 file, 6 tests, PASS.
ESLint: exit 0, no findings.
```

ה־repository-wide assertion היה נכשל על הנתיב שנמצא בביקורת ועל כל שאר נתיבי
הפרופיל שהוסרו. לא נסרק או שונה קובץ binary, ולא בוצעה גישת רשת.

## מטריצת סגירת הביקורת העצמאית — 28.8.2026

כל runtime path מקומי מוצג כאן כ־`<local Node.js 24 directory>`. שלוש הרצות
`verify` נשמרות בנפרד; רק השלישית היא ראיית PASS מלאה.

```text
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run verify
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run build
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run test:e2e:run -- e2e/lifecycle.spec.ts e2e/pagination.spec.ts
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npx --no-install supabase db reset
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run test:e2e:run -- e2e/lifecycle.spec.ts e2e/pagination.spec.ts
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run verify
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run verify
$runtime = '<local Node.js 24 directory>'; $env:PATH = "$runtime;$env:PATH"; npm run build
```

```text
verify attempt 1: FAIL — כל השערים עד Playwright עברו; Playwright סיים 36/38.
נרשם WebServer Error מסוג destination stream closed early; lifecycle Desktop
נכשל בהודעת result transient וב־cleanup, ו־pagination Desktop נכשל ב־registration visibility.

focused attempt before reset: FAIL — 0/4; ה־cleanup הכושל השאיר fixture מקומי
שלא ניתן היה ליצור מחדש, ושתי הרשמות לא הגיעו ל־completion.

local forward reset: PASS — כל 38 migrations עד
20260827200000_slice9_boundary_activation_actor.sql וה־seed הוחלו; stack חזר בריא.

focused attempt after reset: PASS — lifecycle + pagination עברו 4/4 ב־Desktop/Mobile.

verify attempt 2: FAIL — session מקביל התחיל local db reset בזמן test:db.
slice9-time-serialization השלים 64 assertions, ואז container הוחלף ושני קבצים
נותרו ללא חיבור. Docker דיווח OOMKilled=false; הריצה אינה ראיית קוד.

isolated verify attempt 3: PASS — lint; strict typecheck; 50 Vitest files,
639 tests; 31 pgTAP files, 1496 tests; generated types current;
project-book byte regeneration; submission docs; production build;
52-artifact client-secret scan; Playwright 38/38 ב־6.6m ללא WebServer Error.

standalone build (final): PASS — Next.js 16.3.0 compiled, TypeScript passed,
24 application routes listed.
```

הריצות הכושלות אינן מסומנות PASS ואינן נמחקות מן הראיה. הריצה הסופית בוצעה
רק לאחר יציאת תהליך ה־reset/verify המקביל ובדיקת היעדר תהליך DB/E2E מתחרה.
לא בוצעה mutation ב־Hosted/linked ולא נקרא provider חי.

## בדיקות מסמכים ממוקדות לאחר עדכון ה־ledger

הניסיון הראשון לא התחיל: shell לא־interactive לא מצא `node` או `npm`, ולכן
הוא נרשם כ־NO RUN ואינו ראיית בדיקה. לאחר פתרון מפורש של runtime Node.js
המקומי, שלוש הבדיקות הבאות רצו בפועל:

```text
npm run docs:book:check
npm run docs:submission:check
npm run test -- src/lib/repository-path-hygiene.test.ts src/lib/deployment-docs-contract.test.ts src/lib/project-book-contract.test.ts

Book: current and deterministic, PASS.
Submission docs: 15 documents, 34 local links, 4 external links inventoried, PASS.
Focused Vitest: 3 files, 13 tests, PASS.
```

## בחירת בינאריים חיצוניים לתיק ההגשה — 29.8.2026

`VERIFIED` ברשומה זו נשאר מוגבל לסנכרון מסמכי המאגר ולשחזור הדטרמיניסטי של
תוצריו. ה־owner בחר בנפרד את הבינאריים הבאים כתוצרי ההגשה הרשמיים. הבחירה
מקפיאה את bytes המקוריים, אך אינה טוענת שתיק ההגשה או ה־ZIP כבר נוצרו:

| Source basename | Target basename | SHA-256 | Disposition |
| --- | --- | --- | --- |
| `Predictor1_Project_Book_HE_v2.1 (2).pdf` | `Predictor1_Project_Book_HE_v2.1.pdf` | `DBA0AE5F200394A70BDDF65E7229C0443F8D8145D9704096986AA73CB8F5D0EA` | OWNER_APPROVED_BYTES_FROZEN |
| `Predictor1_Project_Book_HE_v2.1 (2) (1).docx` | `Predictor1_Project_Book_HE_v2.1.docx` | `73FB802509CD8D18579079FD05B8B9817C44D1A75543566F09D27581C998318D` | OWNER_APPROVED_BYTES_FROZEN |
| `Predictor1-Final-Project-Upgraded-RTL (2).pptx` | `Predictor1_Final_Presentation_HE.pptx` | `8B805B3C735C14A03BDE2BC3830F011842842549150B7E37A8E7F62C5D40B62C` | OWNER_APPROVED_BYTES_FROZEN |

ה־generators של המאגר מאמתים רק את `docs/project-book.docx` ואת
`presentation/predictor1-final-project.pptx`. אסור להפנות אותם אל הבינאריים
הנבחרים, לייצא אותם מחדש או לשנות אותם. העתקה byte-for-byte, ‏`LINKS.txt`,
הרכבת ה־ZIP ו־extract/reopen/hash/link/secret QA נשארים תחת closeout של
`S9-REQ-003` לאחר final SHA ופרסום Public.
