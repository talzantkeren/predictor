# Database migrations

Add ordered SQL migrations here. Slice 0 starts with foundation extensions and enums. Slice 1 adds private profiles, Auth triggers, column grants and self-only RLS in the identity migration. Exposed tables, grants, RLS policies, and generated types are added with their corresponding slices.
