# Database migrations

Add ordered SQL migrations here. Slice 0 starts with foundation extensions and enums only; exposed tables, grants, RLS policies, and generated types are added with their corresponding slices.
